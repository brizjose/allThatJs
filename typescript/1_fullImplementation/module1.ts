export function doubleUp(n: number): number {
  return n * 2;
}

export function tripleUp(n: number): number {
  return n * 3;
}

export function squareUp(n: number): number {
  return Math.pow(n, 2);
}

export function sqrtOf(n: number): number {
  return Math.sqrt(n);
}
