export function doubleDown(n: number): number {
  return n / 2;
}

export function tripleDown(n: number): number {
  return n / 3;
}

export function logNatural(n: number): number {
  return Math.log(n);
}
