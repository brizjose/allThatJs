"use strict";
// setting function types
// set the structure and return type
var multiply;
function math(v1, v2) {
    return v2 + v2;
}
function greet(arg, name) {
    return arg + " " + name;
}
multiply = math;
// multiply = greet; error!
