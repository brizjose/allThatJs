// to save or pass a stable data structure

let address: [number, string] = [999, 'Elm St'];

// the array with identified types inside makes an enum that will always have two data points, first a number and then a string.
